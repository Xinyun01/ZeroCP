# cpplint 配置文件

# 设置行宽为 120
linelength=120

# 过滤规则（根据需要调整）
filter=-build/c++11
filter=-whitespace/braces
filter=-whitespace/indent
filter=-readability/braces

# 头文件扩展名
headers=h,hpp,hxx,inl

# 根目录
root=.
