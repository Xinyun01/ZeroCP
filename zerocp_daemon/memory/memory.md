MemoryManager (内存管理器)
    │
    ├─ MemPool #1 (内存池 1) ← 管理 128 字节的 Chunk
    │    │
    │    ├─ MpmcLoFFLi (管理区)
    │    │    └─ Index_t[] (索引数组)
    │    │
    │    └─ Chunk Array (数据区)
    │         ├─ Chunk 0 (块 0)
    │         ├─ Chunk 1 (块 1)
    │         ├─ Chunk 2 (块 2)
    │         └─ ... (共 10000 个)
    │
    ├─ MemPool #2 (内存池 2) ← 管理 1024 字节的 Chunk
    │    │
    │    ├─ MpmcLoFFLi (管理区)
    │    │    └─ Index_t[] (索引数组)
    │    │
    │    └─ Chunk Array (数据区)
    │         ├─ Chunk 0
    │         ├─ Chunk 1
    │         └─ ... (共 5000 个)
    │
    └─ MemPool #3 (内存池 3) ← 管理 4096 字节的 Chunk
         │
         ├─ MpmcLoFFLi (管理区)
         │    └─ Index_t[] (索引数组)
         │
         └─ Chunk Array (数据区)
              ├─ Chunk 0
              ├─ Chunk 1
              └─ ... (共 1000 个)



T1: 配置内存管理器
     │
     ├─► MemoryManager::configureMemoryManager()
     │   │
     │   ├─► for (每个配置项) {
     │   │       addMemPool(entry.m_size, entry.m_chunkCount);
     │   │   }
     │   │
     │   └─► generateChunkManagementPool();

T2: 添加第一个内存池（128 字节 × 10000 个）
     │
     ├─► MemoryManager::addMemPool(128, 10000)
     │   │
     │   └─► m_memPoolVector.emplace_back(
     │           128,     // chunkSize
     │           10000,   // numberOfChunks
     │           managementAllocator,
     │           chunkMemoryAllocator
     │       );

T3: 🔥 MemPool[0] 构造（核心初始化）
     │
     ├─► MemPool::MemPool(128, 10000, ...)
     │   │
     │   ├─► 📍 步骤 1: 分配数据块数组
     │   │   m_rawMemory = chunkMemoryAllocator.allocate(
     │   │       10000 * 128,  // 1,280,000 字节
     │   │       8             // 对齐
     │   │   );
     │   │   // 返回: 0x7f9000000000（示例地址）
     │   │   //
     │   │   // 🔥 关键：记录池基址
     │   │   //    m_rawMemory = 0x7f9000000000
     │   │   //    这是索引→指针转换的基准
     │   │
     │   ├─► 📍 步骤 2: 分配空闲索引数组
     │   │   auto* memoryFreeList = managementAllocator.allocate(
     │   │       sizeof(uint32_t) * (10000 + 1),  // 40,004 字节
     │   │       8
     │   │   );
     │   │   // 返回: 0x7f8a00001000（示例地址）
     │   │
     │   └─► 📍 步骤 3: 初始化空闲索引链表
     │       m_freeIndices.init(memoryFreeList, 10000);

T4: 🔥 MpmcLoFFLi::init() - 设置池索引
     │
     ├─► MpmcLoFFLi::init(0x7f8a00001000, 10000)
     │   │
     │   ├─► m_nextFreeIndex = 0x7f8a00001000;
     │   │   m_size = 10000;
     │   │   m_invalidIndex = 10001;
     │   │
     │   └─► 🔥 初始化索引链表：
     │       for (i = 0; i < 10001; i++) {
     │           m_nextFreeIndex[i] = i + 1;
     │       }
     │       
     │       内存布局:
     │       ┌─────────────────────────────────────────┐
     │       │ 地址: 0x7f8a00001000                    │
     │       ├─────────────────────────────────────────┤
     │       │ [0] = 1      ← 第一个空闲索引          │
     │       │ [1] = 2                                 │
     │       │ [2] = 3                                 │
     │       │ [3] = 4                                 │
     │       │ ...                                     │
     │       │ [9998] = 9999                           │
     │       │ [9999] = 10000                          │
     │       │ [10000] = 10001  ← 终止标记             │
     │       └─────────────────────────────────────────┘

T5: 添加第二个内存池（1024 字节 × 5000 个）
     │
     ├─► MemoryManager::addMemPool(1024, 5000)
     │   └─► MemPool::MemPool(1024, 5000, ...)
     │       │
     │       ├─► m_rawMemory = chunkMemoryAllocator.allocate(
     │       │       5000 * 1024  // 5,120,000 字节
     │       │   );
     │       │   // 返回: 0x7f9000138800（紧接着池0）
     │       │
     │       └─► m_freeIndices.init(memoryFreeList, 5000);
     │           // 初始化索引: 0→1→2→...→4999→5000(终止)

T6: 重复添加所有配置的池
     │
     ├─► Pool[2]: 16KB × 1000
     ├─► Pool[3]: 128KB × 200
     ├─► Pool[4]: 512KB × 50
     └─► ...

T7: 生成 ChunkManagement 池
     │
     └─► MemoryManager::generateChunkManagementPool()
         └─► m_chunkManagementPool.emplace_back(
                 sizeof(ChunkManagement),  // 48 字节
                 totalChunkCount,          // 所有池的总 Chunk 数
                 managementAllocator,
                 managementAllocator
             );